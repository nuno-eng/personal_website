{
  "name": "site-media",
  "private": true,
  "type": "module",
  "description": "Builds the site photos and social share images from ../../photos (originals, not committed)",
  "scripts": { "build": "node build.mjs" },
  "dependencies": { "puppeteer-core": "^24.0.0" }
}
